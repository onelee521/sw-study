server:
  port: 8001

spring:
  application:
    name: user-service

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    eureka-service-url:
      defaultZone: http://localhost:8761/eureka
